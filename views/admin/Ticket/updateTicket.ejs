<%- include ../../partials/header.ejs %> <%- include ../../partials/navbar.ejs
%> <%- include ../../partials/sidebar.ejs %>

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
  <!-- Content Header (Page header) -->
  <section class="content-header">
    <div class="container-fluid">
      <div class="row mb-2">
        <div class="col-sm-6">
          <h1>Ticket</h1>
        </div>
        <div class="col-sm-6">
          <ol class="breadcrumb float-sm-right">
            <li class="breadcrumb-item"><a href="/ticket">Home</a></li>
            <li class="breadcrumb-item active">Update</li>
          </ol>
        </div>
      </div>
    </div>
    <!-- /.container-fluid -->
  </section>

  <!-- Main content -->
  <section class="content">
    <div class="container-fluid">
      <!-- general form elements -->
      <div class="card card-primary">
        <div class="card-header">
          <h3 class="card-title">Update Ticket</h3>
        </div>
        <!-- /.card-header -->
        <!-- form start -->
        <form
          action="/ticket/update/<%= ticket._id %>?_method=PUT"
          method="POST"
        >
          <div class="card-body">
            <div class="form-group">
              <label for="film">Film</label>
              <div class="select2-purple">
                <select
                  class="form-control select2"
                  name="film"
                  id="film"
                  disabled
                >
                  <option value="<%= ticket.film._id %>" selected>
                    <%= ticket.film.title %>
                  </option>
                </select>
              </div>
            </div>
            <div class="form-group">
              <label for="nominal">Nominal</label>
              <div class="select2-purple">
                <select
                  class="form-control select2"
                  name="nominal"
                  multiple="multiple"
                  id="nominal"
                  disabled
                >
                  <option value="<%= ticket.nominal._id %>" selected>
                    <%= ticket.nominal.typeName %> | <%= ticket.nominal.price %>
                  </option>
                </select>
              </div>
            </div>
            <div class="form-group">
              <label for="seatNumber">Amount Seat</label>
              <input
                type="text"
                class="form-control"
                name="seatNumber"
                id="seatNumber"
                placeholder="Enter amount seat"
                value="<%= ticket.seatNumber %>"
                required
              />
            </div>
          </div>
          <!-- /.card-body -->

          <div class="card-footer">
            <button type="submit" class="btn btn-primary">Submit</button>
          </div>
        </form>
      </div>
      <!-- /.card -->
      <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
  </section>
  <!-- /.content -->
</div>
<!-- /.content-wrapper -->

<%- include ../../partials/footer.ejs %>
<!-- ./wrapper -->
<%- include ../../partials/javascript.ejs %>
